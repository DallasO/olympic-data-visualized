"""
Django secret settings for mysite project.

Remove .example extension and complete info below
"""

import os

# SECURITY WARNING: keep the secret key used in production secret!
# 50 Character unique field
SECRET_KEY = '##################################################'

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': os.path.join(BASE_DIR, 'db.sqlite3'),
    }
}
